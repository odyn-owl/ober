<h1>New  menu group</h1>

<% form_for(@menu_item) do |f| %>
  <%= f.error_messages %>
  <p>
    name: <%=  f.text_field(:name) %>
  </p>
  
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', menu_items_path %>
